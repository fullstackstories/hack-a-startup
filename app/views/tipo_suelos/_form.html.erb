<%= form_with(model: tipo_suelo, local: true) do |form| %>
  <% if tipo_suelo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_suelo.errors.count, "error") %> prohibited this tipo_suelo from being saved:</h2>

      <ul>
      <% tipo_suelo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
